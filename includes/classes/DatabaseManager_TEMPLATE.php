<?php

/**
 * Class DatabaseManager_TEMPLATE
 * This is simply a template and needs to be renamed to DatabaseManager. This is done it's not part of the version control system... we don't want the actual
 * database credentials to appear on GitHub! Essentially, DatabaseManager is ignored during pushes and will only remain on the local repo.
 * The constants below are simply examplary
 */
class DatabaseManager_TEMPLATE
{

    /**
     * Server host
     */
    const host = "localhost";
    /**
     * Database name
     */
    const dbName = "";
    /**
     * Database user
     */
    const dbUser = "root";
    /**
     * Database port
     */
    const dbPort = 3306;
    /**
     * Database password
     */
    const dbPass = "";
}